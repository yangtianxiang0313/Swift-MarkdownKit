---
description: 问题修复流程——根因分析、用户确认、正向修复，禁止 trick 或回退
alwaysApply: true
---

# 问题修复流程

修复 Bug 时必须遵循以下流程，**禁止**用 trick 或回退方式敷衍处理。

## 1. 根因分析

先分析问题**出现的根本原因**，而非表象。

- 追溯调用链、数据流、状态变化，定位为何会走到错误分支
- 区分「直接原因」与「根因」：例如「某处没调用 reveal」是直接原因，「为何设计上依赖该调用却没保证」才是根因
- 若不确定根因，列出候选假设和验证思路，再继续

## 2. 与用户确认

在动手修复前，**与用户确认根因和修复思路**。

- 简明说明：问题根因、拟采用的修复方向、预期结果
- 如有多种方案，说明取舍理由，请用户选择或认可
- 仅在用户明确同意或已给出方向后，再实施代码修改

## 3. 正向修复

必须以**正向、根治**方式解决问题，不得采用取巧或回退。

| 禁止 | 说明 | 正确替代 |
|------|------|----------|
| **Trick 修复** | 绕过问题、打补丁、特殊分支、隐式假设 | 修正设计或实现中的缺陷，使逻辑自洽 |
| **回退修复** | 撤销改动、恢复旧实现、放弃新能力 | 找出新实现的问题并修复，保留正确行为 |
| **临时方案** | 注释、条件开关、TODO、兼容层 | 直达目标，一次到位 |
| **隐藏问题** | 吞错误、静默降级、用户无感知的折中 | 暴露问题或明确降级策略，可观测、可排查 |

## 4. 示例

**错误做法：**
- 用户反馈「每段只显示首字」→ 直接加一行 `reveal()` 调用，不解释根因
- 用户反馈「一个字都不显示」→ 回退到旧配置，放弃新动画能力

**正确做法：**
- 分析：`updateContent` 只更新策略内部状态，`reveal()` 负责写入 View；Instant 驱动在 update 路径未调用 `reveal`，属设计遗漏 → 与用户确认根因 → 在「update 后应触发 reveal」的正确位置补全调用
- 若新动画系统导致无显示：分析 StreamAnimator 的视图创建、tick 时序、alpha/布局等，定位根因 → 与用户确认 → 修正 StreamAnimator 实现，而非回退配置
