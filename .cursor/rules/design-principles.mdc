---
description: XHSMarkdownKit 设计原则：数据→规则→视图管线、全层可替换、依赖倒置、最优方案优先
alwaysApply: true
---

# 设计原则

## 项目本质

XHSMarkdownKit 的本质是 **「原始数据 → 规则 → 视图」管线**。Markdown 只是默认的数据格式和默认的规则集，框架本身不绑定 Markdown。

- **每一层的具体逻辑都是用户实现的，框架只提供默认选项**
- Parser（解析什么格式）、Renderer（怎么转 Fragment）、ViewStrategy（用什么 View）、Spacing（间距多大）、HeightEstimatable（高度怎么算）—— 全部可替换
- 框架的价值是管线编排、协议定义、默认实现，不是硬编码的具体逻辑
- 用户可以只用 Markdown 子集、可以混入自定义节点、可以完全不用 Markdown —— 框架都应支持

## 项目定位

高性能、高可配置性、流式支持、动画支持的基础设施级渲染库。

- **不是业务库**，不存在「过度设计」——服务于可替换、可配置、高性能、流式/动画的设计投入都是必要的
- 不得以「当前用不到」「够用就行」为由拒绝更完备的架构
- **自定义节点是常态**：新增超标准节点（可折叠区域、投票卡片等）是常态操作，设计时以此为基准场景

## 核心原则

### 1. 全层可替换，框架只编排

框架定义管线和协议，具体实现全部可由用户替换。设计每一层时验证：

- **替换**：用户能否只替换这一层的实现，而不动其他层？
- **新增**：新增一个全新节点类型，用户需要做多少事？框架需要改几行？（答案应为零）
- **裁剪**：用户能否只用部分能力（如只要解析不要动画、只要子集节点）？

### 2. 依赖倒置，零侵入扩展

框架依赖用户注入的协议实现，而非框架内部做类型分支。

- **禁止** `switch nodeType` 穷举、`as? 具体Fragment类型` 等框架内部的类型分支
- 能力通过协议注入：MarkdownParser、ViewStrategy、BlockSpacingResolving、HeightEstimatable 等
- 新增自定义节点时，用户只需实现 Renderer + View 并注册，框架零修改

### 3. 最优方案，不折中

- 直达最终形态，不写「先这样，以后再优化」
- 不做兼容层、不保留废弃 API、不提供过渡逻辑
- 项目无需后向兼容，破坏性更新（Breaking Changes）可接受
- **最优 ≠ 简化**：不以牺牲扩展性换取代码更短，不以牺牲文档/注释换取行数更少

### 4. 协议优先于闭包

优先用 protocol 表达能力，闭包仅用于极简一次性回调（如 completion）。

### 5. 必要能力强制实现，不兜底

框架所需的能力必须由协议强制要求，不提供隐式兜底。

- 所有参与管线的 View **必须** 实现 `HeightEstimatable`，不接受 `sizeThatFits` 或 `estimatedSize` 静默兜底
- 协议方法的参数必须显式传入所有必要信息，不允许内部"猜测"或"用默认值填充"
- 如果某个能力是可选的，用独立的 opt-in 协议表达（如 `StreamableContent`、`MergeableFragment`），而非在必选协议中给默认实现

## 实施准则

- 删除代码直接移除，不留 `@available`、`deprecated`
- 修改 API 直接改签名，不提供重载兼容旧用法
- 技术讨论不引入「短期/长期」折中，只采纳最优方案
