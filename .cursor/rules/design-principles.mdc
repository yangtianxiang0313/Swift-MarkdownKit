---
description: 设计时永远按最优方案考虑，不做过渡妥协；用户扩展时依赖倒置
alwaysApply: true
---

# 最优方案优先

设计和技术选型时，**永远按最优方案考虑**，不做短期折中、不预留过渡路径。

## 最优 ≠ 简化

**最优方案不是简化方案**，二者是两个概念。

- 不以牺牲**扩展性**（可替换、可接入、可配置）换取代码更短
- 不以牺牲**说明内容**（文档、注释、设计 rationale）换取行数更少
- 最优 = 在满足设计目标的前提下，综合权衡简洁、可扩展、可维护、可说明后的最佳选择

## 依赖倒置

**用户扩展时不应修改框架内部代码**。新增 View、Fragment、Renderer 等时，框架内部不应新增分支或 switch。

- **依赖倒置**：框架依赖用户注入的能力（协议实现、工厂类型），而非框架内部根据类型分支
- **创建时注入**：用户在创建 Fragment / 注册 Renderer 时传入 FragmentHeightProvider、ViewFactory 等协议实现，框架只做调用
- **零侵入扩展**：用户新增自定义类型后，只需在**自己的** Renderer / 工厂中传入协议实现，无需改框架任何内部实现

## 少用闭包，多用协议

**优先用 protocol 表达能力**，避免用闭包传参。

- 闭包可读性差、难以命名、不便扩展；协议语义清晰、可复用、易测试
- 如 `heightCalculator: (Int, CGFloat, MarkdownTheme) -> CGFloat` 改为 `FragmentHeightProvider` 协议；`revealStrategyFactory: (T) -> S` 改为 `TextRevealStrategyProvider` 协议
- 仅在极简、一次性回调（如 completion）时可酌情用闭包

## 原则

- **直达目标**：方案直接实现最终形态，不写「先这样，以后再优化」
- **无过渡层**：不做兼容层、不保留废弃 API、不提供「可选/默认」的过渡逻辑
- **破坏性可接受**：项目无需后向兼容，可做破坏性更新（Breaking Changes）
- **综合权衡**：在满足设计目标前提下，平衡简洁与扩展性、可说明性，选择最优实现

## 实施时

- 删除代码时直接移除，不留 `@available`、`deprecated` 标记
- 修改 API 时直接改签名，不提供重载或默认参数以兼容旧用法
- 技术方案讨论中，不引入「短期方案 / 长期方案」的折中，只采纳最优方案
